// ==============================|| MINIMAL LAYOUT ||============================== //

import React from 'react';

const MinimalLayout = ({ children }) => <React.Fragment>{children}</React.Fragment>;

export default MinimalLayout;
